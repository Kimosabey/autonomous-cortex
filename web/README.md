# Autonomous Cortex web UI

Chat-style investigation UI consuming `POST /v1/investigate` as **Server-Sent Events**. Dev proxy targets port **8104**.

Set `OLLAMA_BASE_URL` and `CORTEX_MODEL` on the API for live synthesis.
